name: Bump Versions
run-name: Bumping versions with GitHub Actions 🚀
on: [push]
jobs:
  bump-rust:
    runs-on: ubuntu-latest
      - name: Checkout repository
        uses: actions/checkout@v3   steps:
      - name: Bump rust version in `rust-toolchain.toml` file
        uses: a-kenji/update-rust-toolchain@v1.1
